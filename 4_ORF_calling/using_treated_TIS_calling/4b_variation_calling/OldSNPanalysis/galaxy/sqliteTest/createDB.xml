<tool id="createDB_test1" name="Create an SQLite database" version="1.0">
    
    <description>to see if we can!</description>
    
    <command interpreter="bash">
        
        createDB --output "${database_name}" --name $table_name
        
    </command>
    
    <inputs>
        <param type="text" size="10" value="batman" name="table_name" label="enter the table name" />
    </inputs>
    
    <outputs>
        <data format="txt" name="database_name" />
    </outputs>
    
    <help>

.. class:: infomark

**What it does**

This tool creates a simple SQLite database with one table (name provided by the user).

-----

.. class:: infomark

**Output**

If everything goes well, this tool should result in an SQLite database with one table called 'batman'.

    </help>
    
</tool>